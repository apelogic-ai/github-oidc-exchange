The issuer is fail-closed until image.repository, image.digest, policy ConfigMap, keyring Secret,
service account, issuer URL, exchange audience, and ingress CIDRs are supplied. The chart grants
that service account namespaced Kubernetes Lease replay-ledger access; no replay table or IRSA
annotation is required.
{{- if .Values.workloadExchange.enabled }}

Workload exchange is HTTPS-only at:
https://{{ include "github-oidc-exchange.name" . }}.{{ .Release.Namespace }}.svc.cluster.local:8443/v1/workload/exchange
Its server certificate must include {{ include "github-oidc-exchange.name" . }}.{{ .Release.Namespace }}.svc.cluster.local.
{{- end }}
